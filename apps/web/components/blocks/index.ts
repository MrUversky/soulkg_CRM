/**
 * Blocks - Composed Components
 * 
 * Composed components built from primitives (ui/).
 * These components combine multiple primitives to create reusable patterns.
 * 
 * Examples:
 * - FormField (Input + Label + Error)
 * - DataTable (Table + Pagination + Filters)
 * - StatusBadge (Badge with status colors)
 */

// Blocks will be added here during migration


