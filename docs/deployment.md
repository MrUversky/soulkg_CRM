# Руководство по развертыванию

Полное руководство по развертыванию Soul KG CRM в production.

## 🎯 Рекомендуемая архитектура

**Frontend (Next.js)** → **Vercel**  
**Backend API (Express)** → **Railway** или **Render**  
**База данных (PostgreSQL)** → **Neon** или **Supabase**

---

## 📋 Вариант 1: Vercel + Railway + Neon (Рекомендуется)

### Преимущества
- ✅ Бесплатные планы для всех сервисов
- ✅ Автоматический деплой из Git
- ✅ Отличная производительность
- ✅ Простая настройка

### Шаг 1: Настройка базы данных (Neon)

1. **Создайте аккаунт на [Neon](https://neon.tech/)**
   - Перейдите на https://neon.tech
   - Зарегистрируйтесь (можно через GitHub)
   - Создайте новый проект

2. **Скопируйте connection string**
   - В дашборде Neon найдите "Connection String"
   - Формат: `postgresql://user:password@host/database?sslmode=require`
   - Сохраните его - понадобится для API

3. **Примените миграции**
   ```bash
   # Локально с connection string от Neon
   cd packages/database
   DATABASE_URL="postgresql://..." npm run db:migrate
   ```

### Шаг 2: Развертывание API (Railway)

1. **Создайте аккаунт на [Railway](https://railway.app/)**
   - Перейдите на https://railway.app
   - Зарегистрируйтесь через GitHub

2. **Создайте новый проект**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий

3. **Настройте сервис**
   - Railway автоматически определит Dockerfile
   - Или выберите "Docker" и укажите путь: `apps/api/Dockerfile`

4. **Настройте переменные окружения**
   В Railway Dashboard → Variables добавьте:
   ```env
   DATABASE_URL=postgresql://... (из Neon)
   PORT=3001
   NODE_ENV=production
   JWT_SECRET=<сгенерируйте: openssl rand -base64 32>
   JWT_REFRESH_SECRET=<сгенерируйте: openssl rand -base64 32>
   JWT_EXPIRES_IN=15m
   JWT_REFRESH_EXPIRES_IN=7d
   OPENROUTER_API_KEY=<ваш ключ, если используется>
   ```

5. **Деплой**
   - Railway автоматически задеплоит при push в main
   - Или нажмите "Deploy" вручную
   - Скопируйте URL вашего API (например: `https://api-production.up.railway.app`)

### Шаг 3: Развертывание Frontend (Vercel)

1. **Создайте аккаунт на [Vercel](https://vercel.com/)**
   - Перейдите на https://vercel.com
   - Зарегистрируйтесь через GitHub

2. **Импортируйте проект**
   - Нажмите "Add New Project"
   - Выберите ваш репозиторий
   - Vercel автоматически определит Next.js

3. **Настройте проект**
   - **Root Directory**: `apps/web`
   - **Framework Preset**: Next.js
   - **Build Command**: `npm run build` (или оставьте по умолчанию)
   - **Output Directory**: `.next`

4. **Настройте переменные окружения**
   В Vercel Dashboard → Settings → Environment Variables:
   ```env
   NEXT_PUBLIC_API_URL=https://your-api-url.railway.app
   ```

5. **Деплой**
   - Vercel автоматически задеплоит при push в main
   - Или нажмите "Deploy" вручную
   - Получите URL вашего приложения (например: `https://soul-kg-crm.vercel.app`)

### Шаг 4: Проверка

1. **Проверьте API**
   ```bash
   curl https://your-api-url.railway.app/health
   # Должен вернуть: {"status":"ok","timestamp":"..."}
   ```

2. **Проверьте Frontend**
   - Откройте URL от Vercel
   - Попробуйте зарегистрироваться
   - Проверьте работу API запросов

---

## 📋 Вариант 2: Vercel + Render + Neon

### Преимущества
- ✅ Бесплатный план Render
- ✅ Простая настройка

### Развертывание API на Render

1. **Создайте аккаунт на [Render](https://render.com/)**
   - Перейдите на https://render.com
   - Зарегистрируйтесь через GitHub

2. **Создайте Web Service**
   - Нажмите "New +" → "Web Service"
   - Подключите ваш репозиторий

3. **Настройте сервис**
   - **Name**: `soul-kg-crm-api`
   - **Environment**: Docker
   - **Dockerfile Path**: `apps/api/Dockerfile`
   - **Docker Context**: `.` (корень репозитория)
   - **Plan**: Free

4. **Настройте переменные окружения**
   В разделе "Environment":
   ```env
   DATABASE_URL=postgresql://... (из Neon)
   PORT=3001
   NODE_ENV=production
   JWT_SECRET=<сгенерируйте>
   JWT_REFRESH_SECRET=<сгенерируйте>
   JWT_EXPIRES_IN=15m
   JWT_REFRESH_EXPIRES_IN=7d
   ```

5. **Деплой**
   - Render автоматически задеплоит
   - Скопируйте URL (например: `https://soul-kg-crm-api.onrender.com`)

**Примечание**: На бесплатном плане Render сервисы "засыпают" после 15 минут бездействия. Первый запрос может занять 30-60 секунд.

---

## 📋 Вариант 3: Полный стек на Railway

### Преимущества
- ✅ Все в одном месте
- ✅ Простая настройка
- ✅ Встроенная PostgreSQL

### Развертывание

1. **Создайте проект на Railway**

2. **Добавьте PostgreSQL**
   - Нажмите "+ New" → "Database" → "Add PostgreSQL"
   - Railway создаст БД автоматически
   - Скопируйте `DATABASE_URL` из переменных окружения

3. **Добавьте API сервис**
   - Нажмите "+ New" → "GitHub Repo"
   - Выберите репозиторий
   - Railway определит Dockerfile автоматически

4. **Настройте переменные окружения** (как в Варианте 1)

5. **Добавьте Frontend**
   - Можно развернуть на Vercel (как в Варианте 1)
   - Или создать отдельный сервис на Railway (но Vercel лучше для Next.js)

---

## 🔧 Настройка переменных окружения

### Backend (API)

```env
# Обязательные
DATABASE_URL=postgresql://user:password@host/database?sslmode=require
PORT=3001
NODE_ENV=production
JWT_SECRET=<сгенерируйте секретный ключ>
JWT_REFRESH_SECRET=<сгенерируйте секретный ключ>

# Опциональные
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
OPENROUTER_API_KEY=<ваш ключ OpenRouter>
REDIS_HOST=<если используется Redis>
REDIS_PORT=6379
```

**Генерация секретных ключей**:
```bash
openssl rand -base64 32
```

### Frontend (Next.js)

```env
NEXT_PUBLIC_API_URL=https://your-api-url.railway.app
```

---

## 🚀 CI/CD настройка

Проект уже включает GitHub Actions workflow (`.github/workflows/deploy.yml`), который:
- ✅ Запускает линтер
- ✅ Запускает тесты
- ✅ Проверяет сборку

Для автоматического деплоя:
- **Vercel**: Автоматически деплоит при push в main
- **Railway**: Автоматически деплоит при push в main (если подключен репозиторий)
- **Render**: Автоматически деплоит при push в main (если включен Auto-Deploy)

---

## 🔍 Проверка работоспособности

### 1. Проверка API

```bash
# Health check
curl https://your-api-url.railway.app/health

# Ожидаемый ответ:
# {"status":"ok","timestamp":"2025-01-01T00:00:00.000Z"}
```

### 2. Проверка Frontend

1. Откройте URL от Vercel
2. Попробуйте зарегистрироваться
3. Проверьте консоль браузера на ошибки
4. Проверьте Network tab - все запросы должны идти на ваш API

### 3. Проверка базы данных

```bash
# Подключитесь к Neon через psql
psql "postgresql://user:password@host/database?sslmode=require"

# Проверьте таблицы
\dt

# Проверьте данные
SELECT COUNT(*) FROM organizations;
```

---

## 🐛 Решение проблем

### API не запускается

1. **Проверьте логи**
   - Railway: Dashboard → Deployments → View Logs
   - Render: Dashboard → Logs

2. **Проверьте переменные окружения**
   - Убедитесь, что `DATABASE_URL` правильный
   - Проверьте формат connection string

3. **Проверьте порт**
   - Railway/Render автоматически устанавливают `PORT`
   - Убедитесь, что код использует `process.env.PORT`

### Frontend не подключается к API

1. **Проверьте CORS**
   - API должен разрешать запросы с домена Vercel
   - Проверьте настройки CORS в `apps/api/src/index.ts`

2. **Проверьте переменную окружения**
   - `NEXT_PUBLIC_API_URL` должен быть установлен в Vercel
   - URL должен быть без `/api` в конце (добавляется автоматически)

3. **Проверьте сеть**
   - Откройте DevTools → Network
   - Проверьте, куда идут запросы

### База данных недоступна

1. **Проверьте connection string**
   - Формат должен быть правильным
   - SSL должен быть включен (`?sslmode=require`)

2. **Проверьте миграции**
   - Убедитесь, что миграции применены
   - Можно применить локально с production connection string

---

## 📊 Мониторинг

### Railway
- Dashboard показывает метрики CPU, Memory, Network
- Логи доступны в реальном времени

### Vercel
- Analytics доступны в Dashboard
- Логи доступны в разделе Logs

### Neon
- Dashboard показывает использование БД
- Query Analytics доступен на Pro плане

---

## 🔒 Безопасность

### Рекомендации

1. **Секретные ключи**
   - ✅ Никогда не коммитьте `.env` файлы
   - ✅ Используйте переменные окружения в платформах
   - ✅ Регулярно ротируйте JWT секреты

2. **HTTPS**
   - ✅ Все платформы автоматически предоставляют HTTPS
   - ✅ Используйте SSL для подключения к БД

3. **CORS**
   - ✅ Настройте правильные CORS политики
   - ✅ Разрешайте только нужные домены

4. **База данных**
   - ✅ Используйте connection pooling
   - ✅ Ограничьте доступ по IP (если возможно)

---

## 💰 Стоимость

### Бесплатные планы

- **Vercel**: Бесплатно (ограничения: 100GB bandwidth, serverless functions)
- **Railway**: $5 кредитов/месяц (достаточно для небольшого проекта)
- **Render**: Бесплатно (сервисы "засыпают" после 15 минут)
- **Neon**: Бесплатно (512 MB storage, 0.5 CPU)

### Когда переходить на платные планы

- **Vercel**: При превышении лимитов бесплатного плана
- **Railway**: При превышении $5 кредитов/месяц
- **Neon**: При необходимости больше storage или CPU

---

## 📚 Дополнительные ресурсы

- [Vercel Documentation](https://vercel.com/docs)
- [Railway Documentation](https://docs.railway.app/)
- [Render Documentation](https://render.com/docs)
- [Neon Documentation](https://neon.tech/docs)

---

**Последнее обновление**: 2025-01-01


