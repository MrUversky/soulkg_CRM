# Frontend Requirements: Soul KG CRM

> **Версия**: 1.0  
> **Дата создания**: 2025-12-22  
> **Статус**: Архитектурные требования и стандарты

## 🎯 Общие принципы

### 1. Современный дизайн
- ✅ Единая дизайн-система с токенами (цвета, spacing, typography)
- ✅ Поддержка темной/светлой темы (автоматическое переключение)
- ✅ Минималистичный, чистый дизайн в стиле современных SaaS
- ✅ Плавные анимации и микроинтеракции
- ✅ Консистентность во всех компонентах

### 2. Классная архитектура
- ✅ Компонентная архитектура (переиспользуемые компоненты)
- ✅ Separation of Concerns (логика, UI, данные разделены)
- ✅ Type Safety (строгая типизация TypeScript)
- ✅ DRY принцип (без дублирования кода)
- ✅ SOLID принципы

### 3. Без костылей
- ✅ Чистый код (понятные имена, небольшие функции)
- ✅ Проверенные паттерны проектирования
- ✅ Правильная обработка ошибок на всех уровнях
- ✅ Валидация данных (клиент + сервер)
- ✅ Тестируемый код

### 4. Responsive Design (Mobile First)
- ✅ Mobile: < 640px (sm) - вертикальная навигация, полноэкранные формы
- ✅ Tablet: 640px - 1024px (md) - боковая панель появляется
- ✅ Desktop: > 1024px (lg) - полная боковая панель, многоколоночные раскладки
- ✅ Touch-friendly элементы управления
- ✅ Оптимизация для медленных соединений

### 5. Accessibility (WCAG 2.1 AA)
- ✅ Semantic HTML (правильные теги: nav, main, article)
- ✅ ARIA атрибуты (aria-label, aria-describedby, aria-live)
- ✅ Keyboard Navigation (полная навигация с клавиатуры)
- ✅ Screen Readers поддержка
- ✅ Color Contrast (минимум 4.5:1 для текста)
- ✅ Focus Management (видимые focus indicators)

### 6. Performance
- ✅ Code Splitting (автоматический через Next.js)
- ✅ Lazy Loading (компоненты и изображения)
- ✅ Image Optimization (Next.js Image компонент)
- ✅ Caching (React Query для серверного состояния)
- ✅ Bundle Size оптимизация

## 📐 Архитектура компонентов

### Структура папок (реализовано)

```
apps/web/
├── app/                      # Next.js App Router
│   ├── (auth)/              # Auth group route
│   │   ├── login/
│   │   └── register/
│   ├── (dashboard)/         # Dashboard group route (protected)
│   │   ├── layout.tsx      # Dashboard layout
│   │   └── dashboard/
│   ├── layout.tsx           # Root layout с AuthProvider
│   └── page.tsx             # Home page
│
├── components/
│   ├── ui/                  # Базовые UI компоненты
│   │   ├── Button.tsx      # ✅ Реализован
│   │   ├── Input.tsx       # ✅ Реализован
│   │   └── Card.tsx        # ✅ Реализован
│   ├── layout/              # Layout компоненты
│   │   ├── Header.tsx       # ✅ Реализован
│   │   ├── Sidebar.tsx     # ✅ Реализован
│   │   └── ProtectedRoute.tsx # ✅ Реализован
│   └── features/            # Feature-specific компоненты
│
├── lib/
│   ├── api/                # API клиенты
│   │   └── api-client.ts   # ✅ Реализован
│   ├── contexts/           # React Contexts
│   │   └── auth-context.tsx # ✅ Реализован
│   ├── hooks/             # Custom hooks
│   └── utils.ts           # ✅ Реализован
│
├── styles/
│   └── design-tokens.css   # ✅ Реализован
│
└── types/                  # TypeScript типы
    ├── auth.ts            # ✅ Реализован
    ├── client.ts          # ✅ Реализован
    ├── user.ts            # ✅ Реализован
    └── organization.ts    # ✅ Реализован
```

## 🎨 Design System

### Цветовая палитра (реализовано)

**Light Theme:**
- Primary: Blue (#3b82f6)
- Background: White (#ffffff)
- Surface: Light Gray (#f9fafb)
- Text Primary: Dark Gray (#111827)
- Text Secondary: Medium Gray (#6b7280)

**Dark Theme:**
- Background: Dark Blue (#0f172a)
- Surface: Slate (#1e293b)
- Text Primary: Light Gray (#f1f5f9)
- Text Secondary: Medium Gray (#cbd5e1)

### Компоненты (реализовано)

1. **Button**
   - Варианты: primary, secondary, outline, ghost, danger
   - Размеры: sm, md, lg
   - Состояния: loading, disabled
   - Accessibility: keyboard navigation, focus styles

2. **Input**
   - Поддержка label, error, helperText
   - Валидация через ARIA
   - Dark mode support

3. **Card**
   - Варианты: default, elevated, outlined
   - Композиция: Header, Title, Body, Footer
   - Responsive padding

## 📱 Responsive Breakpoints

```css
sm: 640px   /* Mobile */
md: 768px   /* Tablet */
lg: 1024px  /* Desktop */
xl: 1280px  /* Wide Desktop */
```

### Mobile First Strategy

1. **Mobile (< 640px)**
   - Hamburger menu
   - Вертикальные списки
   - Полноэкранные формы
   - Touch-friendly кнопки (min 44x44px)

2. **Tablet (640px - 1024px)**
   - Боковая панель появляется
   - Двухколоночные раскладки
   - Адаптивные карточки

3. **Desktop (> 1024px)**
   - Полная боковая панель
   - Многоколоночные раскладки
   - Hover эффекты

## ♿ Accessibility Checklist

### Обязательные требования

- [x] Semantic HTML теги
- [x] ARIA атрибуты где нужно
- [x] Keyboard navigation
- [x] Focus indicators
- [x] Color contrast (4.5:1 минимум)
- [x] Alt text для изображений
- [x] Form labels и error messages
- [x] Screen reader поддержка

### Best Practices

- [x] Логичный порядок табуляции
- [x] Skip links для навигации
- [x] ARIA live regions для динамического контента
- [x] Правильные heading hierarchy

## ⚡ Performance Targets

### Core Web Vitals

- **LCP (Largest Contentful Paint)**: < 2.5s
- **FID (First Input Delay)**: < 100ms
- **CLS (Cumulative Layout Shift)**: < 0.1

### Оптимизации

- [x] Code splitting (Next.js автоматический)
- [x] Image optimization (Next.js Image)
- [ ] Lazy loading компонентов (будет добавлено)
- [x] API client с кэшированием
- [ ] Bundle size анализ (будет добавлено)

## 🧪 Тестирование Strategy

### Типы тестов

1. **Unit Tests** (Vitest)
   - Утилиты и hooks
   - Изолированные компоненты

2. **Integration Tests** (Vitest + React Testing Library)
   - Взаимодействие компонентов
   - Формы и валидация

3. **E2E Tests** (Playwright)
   - Критичные пользовательские сценарии
   - Разные устройства

### Покрытие

- Минимум 70% для критичных компонентов
- 100% для утилит и hooks

## 📚 Используемые библиотеки

### Core
- **Next.js 16+**: Framework с App Router
- **React 19+**: UI библиотека
- **TypeScript**: Строгая типизация

### Styling
- **Tailwind CSS 4**: Utility-first CSS
- **CSS Variables**: Для темизации

### Forms & Validation
- **React Hook Form**: Управление формами
- **Zod**: Валидация схем
- **@hookform/resolvers**: Интеграция Zod с RHF

### State Management
- **React Context**: Глобальное состояние (auth, theme)
- **React Query** (@tanstack/react-query): Серверное состояние

### UI Components
- **Lucide React**: Иконки
- **Custom Components**: Button, Input, Card (созданы)

### Utilities
- **clsx**: Условные классы
- **tailwind-merge**: Merge Tailwind классов

## 🔒 Безопасность Frontend

### Реализовано

1. **XSS Protection**
   - React автоматически экранирует
   - Санитизация пользовательского ввода

2. **Secure Storage**
   - Токены в localStorage (с защитой от XSS)
   - HttpOnly cookies (можно добавить позже)

3. **CSRF Protection**
   - SameSite cookies
   - CSRF токены для критичных операций (будет добавлено)

## 📝 Code Standards

### TypeScript
- ✅ Строгий режим (`strict: true`)
- ✅ Минимум `any` типов
- ✅ Явные типы возвращаемых значений

### Naming Conventions
- Компоненты: `PascalCase` (ClientList)
- Hooks: `camelCase` с префиксом `use` (useClients)
- Утилиты: `camelCase` (formatPhone)
- Константы: `UPPER_SNAKE_CASE` (API_URL)

### File Organization
- Один компонент = один файл
- Имя файла = имя компонента
- Index файлы для re-exports

## ✅ Что реализовано

### Этап 6: Frontend аутентификация
- ✅ AuthContext с полным управлением состоянием
- ✅ Страницы login и register
- ✅ ProtectedRoute компонент
- ✅ Автоматическое обновление токенов
- ✅ Дизайн-система с токенами
- ✅ Базовые UI компоненты
- ✅ Layout компоненты (Header, Sidebar)
- ✅ Dashboard layout
- ✅ Responsive дизайн
- ✅ Accessibility поддержка

## 🚀 Следующие шаги

### Этап 7: Frontend CRUD UI
- [ ] Страница списка клиентов с фильтрацией
- [ ] Страница профиля клиента
- [ ] Формы создания/редактирования
- [ ] Страница управления пользователями (ADMIN)
- [ ] Страница настроек организации

---

**Последнее обновление**: 2025-12-22


