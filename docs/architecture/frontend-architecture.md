# Frontend Architecture: Soul KG CRM

> **Версия**: 1.0  
> **Дата создания**: 2025-12-22  
> **Статус**: Архитектурные требования для Frontend

## 🎯 Принципы разработки

### 1. Современный дизайн
- **Design System**: Единая система дизайна с компонентами и токенами
- **Современный UI**: Чистый, минималистичный дизайн в стиле современных SaaS
- **Темная/светлая тема**: Поддержка обеих тем с автоматическим переключением
- **Анимации**: Плавные переходы и микроинтеракции для лучшего UX

### 2. Классная архитектура
- **Компонентная архитектура**: Переиспользуемые, композируемые компоненты
- **Separation of Concerns**: Четкое разделение логики, UI и данных
- **Type Safety**: Строгая типизация TypeScript везде
- **No Code Duplication**: DRY принцип, переиспользование кода
- **SOLID принципы**: Следование принципам SOLID

### 3. Без костылей
- **Чистый код**: Понятные имена, небольшие функции, комментарии где нужно
- **Паттерны проектирования**: Использование проверенных паттернов
- **Error Handling**: Правильная обработка ошибок на всех уровнях
- **Валидация**: Валидация данных на клиенте и сервере
- **Тестируемость**: Код должен быть легко тестируемым

### 4. Мобильная версия (Responsive Design)
- **Mobile First**: Разработка сначала для мобильных, затем для десктопа
- **Breakpoints**: 
  - Mobile: < 640px (sm)
  - Tablet: 640px - 1024px (md)
  - Desktop: > 1024px (lg)
  - Wide: > 1280px (xl)
- **Touch-friendly**: Элементы управления удобны для тач-интерфейсов
- **Performance**: Оптимизация для медленных соединений

### 5. Accessibility (a11y)
- **WCAG 2.1 AA**: Соответствие стандартам доступности
- **Keyboard Navigation**: Полная навигация с клавиатуры
- **Screen Readers**: Поддержка скринридеров (ARIA атрибуты)
- **Color Contrast**: Достаточный контраст цветов
- **Focus Management**: Правильное управление фокусом

### 6. Performance
- **Code Splitting**: Разделение кода по роутам и компонентам
- **Lazy Loading**: Ленивая загрузка компонентов и данных
- **Image Optimization**: Оптимизация изображений (Next.js Image)
- **Caching**: Кэширование данных и статических ресурсов
- **Bundle Size**: Минимизация размера бандла

### 7. SEO (если применимо)
- **Meta Tags**: Правильные meta теги для каждой страницы
- **Structured Data**: Schema.org разметка где нужно
- **Sitemap**: XML sitemap для поисковых систем

## 📐 Архитектура компонентов

### Структура папок

```
apps/web/
├── app/                      # Next.js App Router
│   ├── (auth)/              # Auth group route
│   │   ├── login/
│   │   └── register/
│   ├── (dashboard)/         # Dashboard group route (protected)
│   │   ├── clients/
│   │   ├── users/
│   │   └── settings/
│   ├── layout.tsx          # Root layout
│   └── page.tsx            # Home page
│
├── components/              # React компоненты
│   ├── ui/                 # Primitives - базовые UI компоненты (shadcn/ui)
│   │   ├── Button.tsx      # ✅ Реализован (shadcn/ui + кастомизация)
│   │   ├── Input.tsx       # ✅ Реализован (shadcn/ui + кастомизация)
│   │   ├── Card.tsx        # ✅ Реализован (shadcn/ui + кастомизация)
│   │   ├── label.tsx       # ✅ Реализован (shadcn/ui)
│   │   └── index.ts         # Re-exports всех primitives
│   ├── blocks/             # Composed components (композиция primitives)
│   │   └── index.ts         # Пока пусто, будет расширяться
│   ├── features/           # Feature-specific компоненты (используют ui/ и blocks/)
│   │   ├── clients/        # ✅ ClientList, ClientForm, ClientDetail
│   │   ├── users/          # ✅ UserList, UserForm
│   │   └── settings/       # ✅ OrganizationSettings
│   └── layout/             # Layout компоненты (Header, Sidebar, ProtectedRoute)
│
├── lib/                     # Утилиты и хелперы
│   ├── api/                # API клиенты
│   ├── hooks/              # Custom React hooks
│   ├── utils/              # Утилиты
│   └── constants/         # Константы
│
├── styles/                  # Стили
│   ├── design-tokens.css   # ✅ Design tokens (цвета, типографика, spacing, shadows, radius, animations)
│   └── (globals.css в app/) # Глобальные стили с @theme inline для Tailwind
│
├── types/                   # TypeScript типы
│   ├── api.ts              # API типы
│   ├── components.ts        # Типы компонентов
│   └── index.ts            # Re-exports
│
└── config/                  # Конфигурация
    ├── tailwind.config.ts
    └── tsconfig.json
```

### Компонентная иерархия

```
App Layout
├── AuthProvider (Context)
├── ThemeProvider (Context)
├── Navigation
│   ├── Header
│   │   ├── Logo
│   │   ├── NavLinks
│   │   └── UserMenu
│   └── Sidebar (Desktop)
│       └── NavItems
└── Main Content
    ├── Page Layout
    │   ├── PageHeader
    │   ├── PageContent
    │   └── PageFooter
    └── Feature Components
        ├── ClientList
        │   ├── ClientCard
        │   ├── ClientFilters
        │   └── ClientPagination
        └── ClientDetail
            ├── ClientInfo
            ├── ClientStatus
            └── ClientHistory
```

## 🎨 Design System

> **Полная документация**: `docs/design-system.md`  
> **Токены определены в**: `apps/web/styles/design-tokens.css`  
> **Интеграция с Tailwind**: `apps/web/app/globals.css` (через @theme inline)

### Архитектура дизайн-системы

Дизайн-система построена на основе **CSS переменных (токенов)** и интегрирована с Tailwind CSS через `@theme inline`. Это позволяет:
- ✅ Централизованное управление дизайном
- ✅ Легкое обновление без breaking changes
- ✅ Автоматическое применение изменений везде
- ✅ Поддержка темной/светлой темы

### Цветовая палитра

**Полная палитра с оттенками 50-950:**
- **Primary** (Blue): `--color-primary-50` до `--color-primary-950`
- **Secondary** (Purple): `--color-secondary-50` до `--color-secondary-950`
- **Neutral** (Gray): `--color-neutral-50` до `--color-neutral-950`
- **Semantic**: Success, Warning, Error, Info (каждый с 50-950 оттенками)

**Семантические цвета для компонентов:**
- `--color-background`, `--color-background-subtle`
- `--color-text-primary`, `--color-text-secondary`, `--color-text-tertiary`
- `--color-border`, `--color-surface`, `--color-surface-hover`

**Использование:**
```tsx
// ✅ Правильно - через токены
<div className="bg-background text-text-primary border-border">

// ❌ Неправильно - хардкод цвета
<div className="bg-white text-gray-900 border-gray-200">
```

### Типографика

**Размеры шрифтов:**
- `--text-xs`: 0.75rem (12px)
- `--text-sm`: 0.875rem (14px)
- `--text-base`: 1rem (16px) - **базовый размер**
- `--text-lg`: 1.125rem (18px)
- `--text-xl`: 1.25rem (20px)
- `--text-2xl`: 1.5rem (24px)
- `--text-3xl`: 1.875rem (30px)
- `--text-4xl`: 2.25rem (36px)
- `--text-5xl`: 3rem (48px)
- `--text-6xl`: 3.75rem (60px)

**Веса шрифтов:**
- `--font-light`: 300
- `--font-normal`: 400
- `--font-medium`: 500
- `--font-semibold`: 600
- `--font-bold`: 700

**Line Heights:**
- `--leading-none`: 1
- `--leading-tight`: 1.25 (для заголовков)
- `--leading-snug`: 1.375
- `--leading-normal`: 1.5 (для body текста)
- `--leading-relaxed`: 1.625
- `--leading-loose`: 2

**Letter Spacing:**
- `--tracking-tighter`: -0.05em
- `--tracking-tight`: -0.025em
- `--tracking-normal`: 0em (рекомендуется для читаемости)
- `--tracking-wide`: 0.025em
- `--tracking-wider`: 0.05em
- `--tracking-widest`: 0.1em

**Правила использования:**
- ✅ Использовать `text-base` минимум для body текста
- ✅ Использовать `leading-tight` (1.25) для заголовков
- ✅ Использовать `leading-normal` (1.5) для body текста
- ✅ Избегать `leading-none` (плохая читаемость)
- ✅ Использовать `tracking-normal` для лучшей читаемости

### Spacing System (4px grid)

**Базовые значения:**
- `--space-0`: 0
- `--space-1`: 0.25rem (4px)
- `--space-2`: 0.5rem (8px)
- `--space-3`: 0.75rem (12px)
- `--space-4`: 1rem (16px)
- `--space-5`: 1.25rem (20px)
- `--space-6`: 1.5rem (24px)
- `--space-8`: 2rem (32px)
- `--space-10`: 2.5rem (40px)
- `--space-12`: 3rem (48px)
- `--space-16`: 4rem (64px)
- `--space-20`: 5rem (80px)
- `--space-24`: 6rem (96px)
- `--space-32`: 8rem (128px)

**Правила использования:**
- ✅ Использовать токены spacing (gap-6, p-8, mb-8)
- ✅ Создавать визуальную иерархию через spacing
- ✅ Больше spacing для важных элементов
- ❌ Избегать хардкод значений (gap-[20px], p-[15px])

### Shadows

**Уровни теней:**
- `--shadow-sm`: Легкая тень (0 1px 2px)
- `--shadow-md`: Средняя тень (0 4px 6px)
- `--shadow-lg`: Большая тень (0 10px 15px) - **рекомендуется для карточек**
- `--shadow-xl`: Очень большая тень (0 20px 25px)
- `--shadow-2xl`: Максимальная тень (0 25px 50px)
- `--shadow-inner`: Внутренняя тень

**Правила использования:**
- ✅ Использовать `shadow-lg` для карточек
- ✅ Использовать `shadow-xl` для модальных окон
- ✅ Добавлять `hover:shadow-xl` для интерактивных элементов

### Border Radius

**Радиусы:**
- `--radius-sm`: 0.25rem (4px)
- `--radius-md`: 0.375rem (6px)
- `--radius-lg`: 0.5rem (8px) - **базовый для компонентов**
- `--radius-xl`: 0.75rem (12px) - **рекомендуется для карточек**
- `--radius-2xl`: 1rem (16px)
- `--radius-3xl`: 1.5rem (24px)
- `--radius-full`: 9999px (круглые элементы)

**Правила использования:**
- ✅ Использовать `rounded-lg` для кнопок и инпутов
- ✅ Использовать `rounded-xl` для карточек
- ✅ Использовать `rounded-2xl` для больших карточек

### Анимации и переходы

**Длительности:**
- `--duration-fast`: 150ms
- `--duration-base`: 200ms - **базовая длительность**
- `--duration-slow`: 300ms - **рекомендуется для hover**
- `--duration-slower`: 500ms

**Easing:**
- `--ease-in-out`: cubic-bezier(0.4, 0, 0.2, 1) - **рекомендуется**

**Правила использования:**
- ✅ Использовать `transition-all duration-300` для интерактивных элементов
- ✅ Использовать `ease-in-out` для плавных переходов

### Breakpoints

```css
--breakpoint-sm: 640px;   /* Mobile */
--breakpoint-md: 768px;   /* Tablet */
--breakpoint-lg: 1024px;  /* Desktop */
--breakpoint-xl: 1280px;  /* Wide Desktop */
--breakpoint-2xl: 1536px; /* Extra Wide */
```

## 🏗️ Архитектурные паттерны

### 1. Компонентная иерархия (Primitives → Blocks → Features)

**Правила композиции:**
```
Primitives (ui/) → Blocks (blocks/) → Features (features/)
```

**Правила импорта:**
- ✅ Features компоненты импортируют только из `ui/` и `blocks/`
- ✅ Blocks компоненты импортируют только из `ui/`
- ❌ Features НЕ импортируют напрямую из `ui/` (только через blocks/)
- ❌ Нет прямых стилей в features/ (только через компоненты)

**Пример:**
```typescript
// ✅ Правильно - features использует blocks и ui
import { Button, Card } from '@/components/ui';
import { DataTable } from '@/components/blocks';

// ❌ Неправильно - прямой импорт из ui в features
import { Button } from '@/components/ui/Button'; // Не рекомендуется
```

### 2. Component Composition
```typescript
// Используем композицию вместо пропсов-хеллов
<Card>
  <CardHeader>
    <CardTitle>Title</CardTitle>
    <CardDescription>Description</CardDescription>
  </CardHeader>
  <CardBody>
    Content
  </CardBody>
  <CardFooter>
    Actions
  </CardFooter>
</Card>
```

### 2. Custom Hooks для логики
```typescript
// Выносим бизнес-логику в hooks
const { clients, loading, error, refetch } = useClients({ 
  status: 'QUALIFIED',
  page: 1 
});
```

### 3. Server Components где возможно
```typescript
// Используем Server Components для данных
async function ClientsPage() {
  const clients = await getClients(); // Server Component
  return <ClientList clients={clients} />;
}
```

### 4. Client Components для интерактивности
```typescript
'use client'; // Только когда нужна интерактивность
export function ClientFilters() {
  const [filters, setFilters] = useState();
  // ...
}
```

### 5. Error Boundaries
```typescript
// Обработка ошибок на уровне компонентов
<ErrorBoundary fallback={<ErrorFallback />}>
  <ClientList />
</ErrorBoundary>
```

## 📱 Responsive Design Strategy

### Mobile First Approach

1. **Начинаем с мобильной версии**
   - Простая навигация (hamburger menu)
   - Вертикальные списки
   - Полноэкранные формы

2. **Адаптируем для планшетов**
   - Боковая панель появляется
   - Двухколоночная раскладка где возможно

3. **Оптимизируем для десктопа**
   - Полная боковая панель
   - Многоколоночные раскладки
   - Hover эффекты

### Компоненты должны быть responsive

```typescript
// Пример responsive компонента
<div className="
  grid 
  grid-cols-1          // Mobile: 1 колонка
  md:grid-cols-2        // Tablet: 2 колонки
  lg:grid-cols-3        // Desktop: 3 колонки
  gap-4
">
```

## ♿ Accessibility Requirements

### Обязательные требования

1. **Semantic HTML**
   - Использовать правильные HTML теги (nav, main, article, etc.)
   - Заголовки в правильном порядке (h1 → h2 → h3)

2. **ARIA атрибуты**
   - `aria-label` для иконок без текста
   - `aria-describedby` для форм с подсказками
   - `aria-live` для динамического контента

3. **Keyboard Navigation**
   - Все интерактивные элементы доступны с клавиатуры
   - Логичный порядок табуляции
   - Focus indicators видны

4. **Color Contrast**
   - Минимум 4.5:1 для обычного текста
   - Минимум 3:1 для крупного текста

5. **Alt Text**
   - Все изображения имеют alt текст
   - Декоративные изображения: alt=""

## ⚡ Performance Requirements

### Метрики

- **First Contentful Paint (FCP)**: < 1.8s
- **Largest Contentful Paint (LCP)**: < 2.5s
- **Time to Interactive (TTI)**: < 3.8s
- **Cumulative Layout Shift (CLS)**: < 0.1
- **First Input Delay (FID)**: < 100ms

### Оптимизации

1. **Code Splitting**
   - Автоматический через Next.js
   - Ручной для больших библиотек

2. **Image Optimization**
   - Использовать Next.js Image компонент
   - Lazy loading для изображений ниже fold

3. **Data Fetching**
   - Server Components для статических данных
   - React Query для клиентских данных с кэшированием
   - Оптимистичные обновления где возможно

4. **Bundle Size**
   - Анализ через `@next/bundle-analyzer`
   - Удаление неиспользуемого кода
   - Tree shaking

## 🧪 Тестирование

### Типы тестов

1. **Unit Tests** (Vitest)
   - Тестирование утилит и hooks
   - Тестирование компонентов изолированно

2. **Integration Tests** (Vitest + React Testing Library)
   - Тестирование взаимодействия компонентов
   - Тестирование форм и валидации

3. **E2E Tests** (Playwright)
   - Критичные пользовательские сценарии
   - Тестирование на разных устройствах

### Покрытие

- Минимум 70% для критичных компонентов
- 100% для утилит и hooks

## 📚 Библиотеки и инструменты

### Core
- **Next.js 16+**: Framework
- **React 19+**: UI библиотека
- **TypeScript**: Типизация

### Styling
- **Tailwind CSS 4**: Utility-first CSS с `@theme inline`
- **CSS Variables**: Для темизации (токены в `design-tokens.css`)
- **Интеграция**: Токены доступны через Tailwind классы (`bg-primary`, `text-text-primary`)

### Forms & Validation
- **React Hook Form**: Управление формами
- **Zod**: Валидация схем

### State Management
- **React Context**: Для глобального состояния (auth, theme)
- **React Query**: Для серверного состояния (кэширование, синхронизация)

### UI Components
- **shadcn/ui**: ✅ Интегрирован - базовые компоненты (Button, Input, Card, Label)
  - Компоненты копируются в `components/ui/` (не npm пакет, вы владеете кодом)
  - Кастомизируются под дизайн-систему через токены
  - Используют Radix UI примитивы под капотом
  - Используют CVA (class-variance-authority) для вариантов
  - Конфигурация: `apps/web/components.json`
- **Radix UI**: Используется через shadcn/ui (доступные примитивы)
- **Lucide React**: ✅ Иконки (уже используется)

### Icons
- **Lucide React**: Современные иконки

### Testing
- **Vitest**: Unit и integration тесты
- **React Testing Library**: Тестирование компонентов
- **Playwright**: E2E тесты

## 🔒 Безопасность

### Frontend Security

1. **XSS Protection**
   - Санитизация пользовательского ввода
   - Использование React (автоматическая экранизация)

2. **CSRF Protection**
   - SameSite cookies
   - CSRF токены для критичных операций

3. **Secure Storage**
   - Токены в httpOnly cookies (предпочтительно)
   - Или в localStorage с защитой от XSS

4. **Content Security Policy**
   - Настроить CSP headers

## 📝 Code Standards

### TypeScript
- Строгий режим (`strict: true`)
- No `any` типы (кроме крайних случаев)
- Явные типы возвращаемых значений
- Использование `React.forwardRef` для компонентов с ref

### Naming Conventions
- Компоненты: `PascalCase` (ClientList, Button)
- Файлы компонентов: `PascalCase.tsx` (Button.tsx, Input.tsx)
- Hooks: `camelCase` с префиксом `use` (useClients)
- Утилиты: `camelCase` (formatPhone, cn)
- Константы: `UPPER_SNAKE_CASE` (API_URL)
- CSS переменные: `kebab-case` (--color-primary-600)

### File Organization
- Один компонент = один файл
- Имя файла = имя компонента (PascalCase)
- Index файлы для re-exports (`components/ui/index.ts`)
- Структура: `ui/` → `blocks/` → `features/`

### Comments
- JSDoc для публичных функций и компонентов
- Комментарии для сложной логики
- TODO комментарии с контекстом
- Описание компонентов в начале файла

## 🔧 Правила работы с компонентами

### Создание нового компонента в ui/

**Шаги:**
1. Использовать shadcn/ui если компонент доступен:
   ```bash
   npx shadcn@latest add [component-name]
   ```
2. Кастомизировать под дизайн-систему:
   - Использовать токены вместо хардкод значений
   - Обновить радиусы (`rounded-lg` → `rounded-xl`)
   - Обновить тени (`shadow-md` → `shadow-lg`)
   - Добавить переходы (`transition-all duration-300`)
3. Добавить в `components/ui/index.ts`:
   ```typescript
   export { ComponentName } from './ComponentName';
   export type { ComponentNameProps } from './ComponentName';
   ```

### Обновление существующего компонента

**Безопасный процесс:**
1. Обновить варианты в CVA (если используется)
2. Обновить классы Tailwind (используя токены)
3. Проверить что API не изменился (props остались теми же)
4. Проверить визуально (light/dark mode, responsive)
5. Проверить сборку (`npm run build`)

**Пример безопасного обновления:**
```typescript
// До
size: {
  default: "h-10 px-4 py-2 text-sm"
}

// После (без изменения API)
size: {
  default: "h-12 px-6 py-3 text-base"  // Просто меняем классы
}
// Все страницы автоматически получают новые стили!
```

### Правила использования токенов

**✅ Правильно:**
```tsx
// Использование токенов через Tailwind классы
<div className="bg-background text-text-primary p-8 gap-6">
<Button className="shadow-lg hover:shadow-xl">
```

**❌ Неправильно:**
```tsx
// Хардкод значений
<div className="bg-white text-gray-900 p-[32px] gap-[24px]">
<Button className="shadow-[0_10px_15px_-3px_rgba(0,0,0,0.1)]">
```

### Правила композиции компонентов

**Иерархия импортов:**
```
features/ → blocks/ → ui/
```

**Примеры:**
```typescript
// ✅ Правильно - features использует blocks и ui
import { Button, Card } from '@/components/ui';
import { DataTable } from '@/components/blocks';

// ✅ Правильно - blocks использует ui
import { Button, Input } from '@/components/ui';

// ❌ Неправильно - features напрямую из ui (должно быть через blocks)
import { Button } from '@/components/ui/Button';
```

### Миграция компонентов

**Процесс миграции:**
1. Создать новый компонент в `ui/` (из shadcn/ui)
2. Настроить под дизайн-систему (токены, стили)
3. Заменить импорты в features/
4. Проверить визуально и функционально
5. Удалить старый компонент (если был)

**Важно:** Архитектура позволяет обновлять компоненты без создания легаси, так как:
- Компоненты изолированы в ui/
- Features не знают о внутренних стилях
- Токены через CSS переменные (автоматическое применение)

## 🚀 Deployment Considerations

### Build Optimization
- Production builds минифицированы
- Source maps для production (опционально)
- Environment variables правильно настроены

### Monitoring
- Error tracking (Sentry)
- Analytics (опционально)
- Performance monitoring

## 🔄 Обновление компонентов без легаси

### Принципы безопасного обновления

**Архитектура позволяет обновлять компоненты без создания легаси:**

1. **Изолированные компоненты в ui/**
   - Все обновления в `components/ui/` автоматически применяются везде
   - Features компоненты не знают о внутренних стилях
   - Используют только props компонентов

2. **Токены через CSS переменные**
   - Обновление токенов → автоматическое применение везде
   - Нет зависимости от старых значений
   - Легко обновлять без breaking changes

3. **CVA для вариантов**
   - Обновление вариантов не влияет на API
   - Props остаются теми же
   - Легко менять внутренние стили

### Процесс обновления компонента

**Пример: Обновление Button**

```typescript
// 1. Обновляем варианты в CVA (без изменения API)
const buttonVariants = cva(
  "inline-flex items-center...",
  {
    variants: {
      size: {
        default: "h-10 px-4" → "h-12 px-6",  // Просто меняем классы
        lg: "h-11 px-8" → "h-14 px-10"
      }
    }
  }
)

// 2. Все страницы автоматически получают новые стили
// Никаких изменений в features/ не требуется!
```

**Проверка безопасности:**
- ✅ Компоненты изолированы в ui/
- ✅ Нет прямых стилей в features/
- ✅ Токены через CSS переменные
- ✅ CVA изолирует варианты

### Правила обновления дизайн-токенов

**Безопасные обновления:**
```css
/* ✅ Безопасно - меняем значения токенов */
--text-base: 1rem → 1.125rem
--space-6: 1.5rem → 2rem
--shadow-md: ... → ... (более сильная тень)

/* ✅ Безопасно - добавляем новые токены */
--shadow-3xl: 0 30px 60px ...
```

**Требуют внимания:**
```css
/* ⚠️ Проверить - изменение семантики токенов */
--color-primary: #3b82f6 → #2563eb  /* Может изменить визуал */
```

## 🎯 Современные стандарты дизайна 2025

### Типографика

**Рекомендации:**
- ✅ Минимум `text-base` (16px) для body текста
- ✅ `leading-tight` (1.25) для заголовков
- ✅ `leading-normal` (1.5) для body текста
- ✅ `tracking-normal` для лучшей читаемости
- ❌ Избегать `leading-none` (плохая читаемость)
- ❌ Избегать `tracking-tight` (слишком сжато)

### Размеры компонентов

**Рекомендации:**
- ✅ Кнопки: `h-12` (48px) минимум, `px-6` (24px)
- ✅ Input: `h-12` (48px) минимум
- ✅ Карточки: `p-8` (32px) для больших, `p-6` (24px) для средних
- ✅ Gap между элементами: `gap-6` (24px) минимум

### Визуальная глубина

**Рекомендации:**
- ✅ Карточки: `shadow-lg` или `shadow-xl`
- ✅ Hover эффекты: `hover:shadow-xl`, `hover:scale-[1.02]`
- ✅ Focus states: видимые индикаторы с `ring-2`

### Анимации

**Рекомендации:**
- ✅ `transition-all duration-300` для интерактивных элементов
- ✅ `hover:scale-105` для кнопок
- ✅ Плавные цветовые переходы

## 📚 Дополнительные ресурсы

### Документация
- **Дизайн-система**: `docs/design-system.md` - полное описание всех токенов
- **Frontend требования**: `docs/architecture/frontend-requirements.md`
- **Текущая задача**: `docs/agents/meta-agents/development/CURRENT_TASK.md`

### Референсы дизайна
- **Linear.app**: Современный дизайн для B2B приложений
- **Vercel Dashboard**: Примеры компонентов и типографики
- **Stripe Dashboard**: Профессиональный дизайн для SaaS

### Инструменты
- **shadcn/ui**: https://ui.shadcn.com/
- **Tailwind CSS**: https://tailwindcss.com/
- **Radix UI**: https://www.radix-ui.com/

---

**Последнее обновление**: 2025-12-22  
**Версия документации**: 1.1  
**Изменения**:
- ✅ Добавлена информация о компонентной архитектуре (ui/blocks/features/layout)
- ✅ Добавлено описание дизайн-токенов и их использования
- ✅ Добавлены правила обновления компонентов без легаси
- ✅ Добавлены современные стандарты дизайна 2025
- ✅ Добавлены правила работы с компонентами
- ✅ Добавлена информация об интеграции shadcn/ui

