# Структура агентов

## 📁 Организация

Агенты разделены на два типа с четкой структурой:

```
docs/agents/
├── README.md                    # Обзор всех агентов
├── STRUCTURE.md                 # Этот файл
│
├── meta-agents/                 # Мета-агенты для Cursor
│   ├── README.md
│   └── [agent-name]/
│       ├── instructions.md      # Инструкции для AI
│       ├── documentation.md     # Документация агента
│       ├── workspace/           # Рабочее пространство
│       │   ├── drafts/         # Черновики
│       │   ├── cache/          # Кэш промежуточных результатов
│       │   └── temp/           # Временные файлы (.gitignore)
│       ├── archive/             # Архивы старых версий
│       └── logs/                # Логи работы (опционально)
│
└── system-agents/               # AI агенты системы (будущее)
    ├── README.md
    └── [agent-name]/
        └── documentation.md     # Документация агента
```

## 🔄 Взаимодействие

### Мета-агенты
- Работают последовательно
- Передают артефакты через файлы
- Используют `workspace/cache/` для промежуточных результатов
- Пример: Product Agent → Architecture Agent → Development Agent

### AI агенты системы
- Взаимодействуют через протокол сообщений
- Конфигурации в БД (таблица `agent_configs`)
- Выполняются через Agent Orchestrator

## 📝 Рабочее пространство

Каждый мета-агент имеет:

- **drafts/** - Черновики документов (можно удалять после завершения)
- **cache/** - Кэш промежуточных результатов (может использоваться между запусками)
- **temp/** - Временные файлы (автоматически очищается, не коммитится)
- **archive/** - Архивы старых версий (версионирование: v1.0, v1.1)
- **logs/** - Логи работы (структурированные, JSON формат)

## ✅ Правила

1. Каждый мета-агент в своей папке
2. Рабочее пространство для каждого агента
3. Четкое разделение мета-агентов и системных агентов
4. Все ссылки обновлены на новую структуру

---

**Версия**: 1.0  
**Дата**: 2025-01-27

