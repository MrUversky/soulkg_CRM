# Уточнение концепции агентов

> **⚠️ ВАЖНО**: Этот документ описывает AI агенты системы (qualification-agent и т.д.), которые будут создаваться ПОЗЖЕ.  
> **Сейчас** работаем только с мета-агентами для Cursor. См. [CURRENT_TASK.md](./CURRENT_TASK.md)

## 🤔 Вопрос

Что такое "агент" в нашей системе - код или конфигурация?

**Ответ**: Есть два типа агентов - см. ниже.

## 💡 Правильное понимание

### ⚠️ Два типа агентов

#### 1. Мета-агенты для Cursor (текущий фокус)
Инструкции + шаблоны для AI (меня), как работать.
- Product Agent - создает продуктовую документацию
- Architecture Agent - создает архитектурную документацию
- **Agent Factory Agent** - создает других мета-агентов

#### 2. AI агенты системы (будущее)
Конфигурация в БД для работы с клиентами.
- Qualification Agent - квалифицирует лидов
- Sales Agent - продает туры

---

### AI агенты системы = Конфигурация + Инструкции

AI агенты системы - это **не код**, а:

1. **Конфигурация** (хранится в БД)
   - Системный промпт
   - Настройки LLM (модель, temperature)
   - Список инструментов
   - Правила поведения

2. **Инструкции** (документация)
   - Описание роли и цели
   - Примеры использования
   - Ограничения

3. **Метаданные**
   - Название, описание
   - Версия
   - Статус (active/inactive)

### Код = Общий исполнитель (Orchestrator)

Код существует один для всех агентов:
- **Agent Orchestrator** - выполняет агентов на основе их конфигурации
- Читает конфигурацию из БД
- Применяет системный промпт
- Вызывает инструменты
- Обрабатывает ответы

## 📊 Структура хранения

### В базе данных

```typescript
// Таблица agent_configs
{
  id: string;
  organizationId: string;
  name: string;                    // qualification-agent
  displayName: string;             // Qualification Agent
  description: string;
  
  // Конфигурация
  systemPrompt: string;            // Системный промпт агента
  model: string;                   // gpt-4, claude, etc.
  temperature: number;              // 0.7
  maxTokens: number;               // 2000
  
  // Инструменты (JSON массив)
  tools: [
    {
      name: 'getClientInfo',
      description: 'Получить информацию о клиенте',
      parameters: {...}
    }
  ];
  
  // Правила и ограничения
  rules: string[];                 // Правила поведения
  constraints: string[];            // Ограничения
  
  // Метаданные
  status: 'active' | 'inactive';
  version: string;
  createdAt: Date;
  updatedAt: Date;
}
```

### В файловой системе (документация)

```
docs/agents/
└── system-agents/                  # AI агенты системы
    └── qualification/
        └── documentation.md       # Документация агента
            - Описание роли
            - Примеры использования
            - Ограничения
```

**Примечание**: Мета-агенты для Cursor хранятся в `docs/agents/meta-agents/` с рабочими пространствами.

## 🔄 Как это работает

### Создание агента

1. **Определяем конфигурацию**:
   ```json
   {
     "name": "qualification-agent",
     "systemPrompt": "Ты агент квалификации...",
     "tools": ["getClientInfo", "updateClientStatus"],
     "rules": ["Будь дружелюбным", "Не дави на клиента"]
   }
   ```

2. **Сохраняем в БД** (таблица `agent_configs`)

3. **Создаем документацию** (`docs/agents/system-agents/qualification/documentation.md`)

4. **Готово!** Агент работает через общий Orchestrator

### Выполнение агента

```
Сообщение от клиента
    ↓
Agent Orchestrator читает конфигурацию агента из БД
    ↓
Применяет системный промпт
    ↓
Вызывает LLM с промптом + контекстом
    ↓
LLM решает какие инструменты использовать
    ↓
Orchestrator выполняет инструменты
    ↓
Возвращает ответ клиенту
```

## 🎯 Agent Factory Agent = Генератор конфигураций

### Что он делает

1. **Генерирует системный промпт**
   - На основе шаблона
   - С учетом роли агента
   - С добавлением правил

2. **Определяет инструменты**
   - Какие инструменты нужны агенту
   - Параметры инструментов

3. **Создает конфигурацию**
   - JSON/YAML структура
   - Готова для сохранения в БД

4. **Генерирует документацию**
   - Описание агента
   - Примеры использования

### Что он НЕ делает

- ❌ Не генерирует код (код общий для всех)
- ❌ Не создает классы TypeScript
- ❌ Не пишет обработчики

## 📋 Структура Agent Factory

### Входные данные

```typescript
interface AgentSpec {
  name: string;                    // qualification-agent
  role: string;                    // qualification
  description: string;              // Что делает агент
  capabilities: string[];          // Что умеет делать
  targetStatus?: string;           // Какой статус обновляет
  tools?: string[];                // Какие инструменты использует
}
```

### Выходные данные

1. **Конфигурация** (для БД):
   ```json
   {
     "systemPrompt": "...",
     "model": "gpt-4",
     "temperature": 0.7,
     "tools": [...],
     "rules": [...]
   }
   ```

2. **Документация** (`docs/agents/system-agents/[name]/documentation.md`)

3. **SQL для БД** (INSERT в agent_configs)

## ✅ Преимущества подхода

1. **Гибкость** - легко создавать новых агентов без кода
2. **Простота** - агенты = конфигурация
3. **Версионирование** - можно версионировать промпты
4. **A/B тестирование** - легко тестировать разные промпты
5. **Мультитенантность** - каждая организация может иметь свои агенты

## 🔄 Обновленный процесс

### Создание агента через Factory

```
1. Вы описываете агента (в чате)
   "Создай агента квалификации"

2. Я (AI) генерирую:
   - Системный промпт (на основе шаблона)
   - Конфигурацию (JSON)
   - Документацию (Markdown)
   - SQL для БД (опционально)

3. Сохраняем в БД
   INSERT INTO agent_configs (...)

4. Готово! Агент работает через Orchestrator
```

## 🎯 Итог

**Агенты = Конфигурация (промпты + инструменты + правила)**

**Код = Общий Orchestrator (один для всех агентов)**

**Agent Factory = Генератор конфигураций и документации**

---

**Версия**: 1.0  
**Дата**: 2025-01-27

