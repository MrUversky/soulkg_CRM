# 🔧 Troubleshooting - Railway Deployment

## Проблема: Healthcheck не проходит

Если healthcheck не проходит после деплоя, проверьте следующее:

### 1. Проверьте логи в Railway

1. Зайдите в Railway Dashboard
2. Откройте ваш проект → Deployments
3. Кликните на последний деплой
4. Перейдите в **Logs**

Ищите ошибки типа:
- `Cannot connect to database`
- `Module not found`
- `Port already in use`
- `EADDRINUSE`

### 2. Проверьте переменные окружения

В Railway Dashboard → ваш сервис → Variables проверьте:

**Обязательные переменные**:
```env
DATABASE_URL=postgresql://... (должен быть установлен!)
PORT=3001 (Railway устанавливает автоматически, но проверьте)
NODE_ENV=production
JWT_SECRET=... (должен быть установлен!)
JWT_REFRESH_SECRET=... (должен быть установлен!)
```

**Проверка DATABASE_URL**:
- Формат: `postgresql://user:password@host:port/database?sslmode=require`
- Должен быть из Neon (или другой БД)
- Проверьте, что БД доступна извне

### 3. Проверьте подключение к базе данных

```bash
# Попробуйте подключиться к БД из Railway
# В Railway Dashboard → ваш сервис → Connect → Terminal

# Проверьте переменную окружения
echo $DATABASE_URL

# Попробуйте подключиться
psql $DATABASE_URL -c "SELECT 1;"
```

### 4. Проверьте, что сервер запускается

В логах Railway должны быть строки:
```
🚀 API server running on http://0.0.0.0:3001
📊 Health check available at http://0.0.0.0:3001/health
```

Если их нет - сервер не запускается.

### 5. Проверьте healthcheck вручную

После деплоя попробуйте:
```bash
curl https://your-api-url.railway.app/health
```

Должен вернуть:
```json
{"status":"ok","timestamp":"2025-01-01T00:00:00.000Z"}
```

### 6. Частые проблемы и решения

#### Проблема: "Cannot connect to database"
**Решение**:
- Проверьте `DATABASE_URL` в Railway Variables
- Убедитесь, что БД доступна извне (не localhost)
- Проверьте SSL настройки (`sslmode=require`)

#### Проблема: "Module not found"
**Решение**:
- Убедитесь, что все пакеты собраны в Dockerfile
- Проверьте порядок сборки в Dockerfile
- Проверьте, что `node_modules` скопированы правильно

#### Проблема: "Port already in use"
**Решение**:
- Railway автоматически устанавливает PORT
- Убедитесь, что код использует `process.env.PORT`
- Не хардкодьте порт в коде

#### Проблема: Сервер запускается, но healthcheck не проходит
**Решение**:
- Проверьте, что сервер слушает на `0.0.0.0`, а не `localhost`
- Проверьте healthcheck path в Railway (должен быть `/health`)
- Увеличьте timeout для healthcheck

### 7. Отладка

#### Включите подробное логирование

В `apps/api/src/index.ts` добавьте в начало:
```typescript
console.log('🔍 Environment check:');
console.log('  PORT:', process.env.PORT);
console.log('  NODE_ENV:', process.env.NODE_ENV);
console.log('  DATABASE_URL:', process.env.DATABASE_URL ? 'SET' : 'NOT SET');
```

#### Проверьте, что все модули загружаются

Добавьте логирование при импорте:
```typescript
console.log('📦 Loading routes...');
try {
  const authRoutes = require('./routes/auth.routes').default;
  console.log('✅ Auth routes loaded');
} catch (error) {
  console.error('❌ Failed to load auth routes:', error);
}
```

### 8. Проверка после исправлений

После каждого исправления:
1. Закоммитьте изменения
2. Запушьте в GitHub
3. Дождитесь автоматического деплоя в Railway
4. Проверьте логи
5. Проверьте healthcheck

---

**Если ничего не помогает**:
1. Проверьте Railway Status Page: https://status.railway.app/
2. Создайте новый сервис с нуля
3. Проверьте документацию Railway: https://docs.railway.app/

