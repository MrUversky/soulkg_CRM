# ✅ Проверка работоспособности после деплоя

## 🎯 Быстрая проверка (2 минуты)

### 1. Проверьте Healthcheck

```bash
curl https://your-api-url.railway.app/health
```

**Ожидаемый результат**:
```json
{"status":"ok","timestamp":"2025-01-01T00:00:00.000Z"}
```

✅ Если получили такой ответ - сервер работает!

### 2. Проверьте в Railway Dashboard

1. Зайдите в Railway Dashboard → ваш проект
2. Проверьте статус деплоя:
   - ✅ Должен быть зеленый статус "Active"
   - ✅ Healthcheck должен показывать "Healthy"

### 3. Проверьте логи

Railway Dashboard → ваш сервис → Logs

Ищите:
- ✅ `🚀 API server running on http://0.0.0.0:3001`
- ✅ `📊 Health check available at http://0.0.0.0:3001/health`
- ❌ Нет ошибок типа "Cannot connect to database"

---

## 🔍 Полная проверка (5 минут)

### 1. Проверка API Endpoints

#### Healthcheck
```bash
curl https://your-api-url.railway.app/health
```

#### Регистрация пользователя (тест подключения к БД)
```bash
curl -X POST https://your-api-url.railway.app/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "testpassword123",
    "organizationName": "Test Organization"
  }'
```

**Ожидаемый результат**:
- ✅ `201 Created` - пользователь создан
- ✅ Или `400 Bad Request` - пользователь уже существует (это тоже нормально)

#### Получение списка организаций (требует авторизации)
```bash
# Сначала получите токен из ответа регистрации
TOKEN="your-access-token-here"

curl https://your-api-url.railway.app/api/organizations \
  -H "Authorization: Bearer $TOKEN"
```

### 2. Проверка Frontend

1. Откройте https://soulkg-crm.vercel.app/
2. Откройте DevTools (F12) → Console
3. Проверьте:
   - ✅ Нет ошибок в консоли
   - ✅ Нет ошибок типа "Failed to fetch" или "Network error"
   - ✅ Запросы идут на ваш Railway API URL

4. Попробуйте зарегистрироваться:
   - ✅ Форма работает
   - ✅ Запрос отправляется
   - ✅ Получаете ответ от API

### 3. Проверка базы данных

#### Через Railway Terminal

1. Railway Dashboard → ваш сервис → Connect → Terminal
2. Выполните:
```bash
# Проверьте переменную окружения
echo $DATABASE_URL

# Попробуйте подключиться к БД
psql $DATABASE_URL -c "SELECT COUNT(*) FROM organizations;"
```

**Ожидаемый результат**:
- ✅ Подключение успешно
- ✅ Видите количество организаций (может быть 0, это нормально)

#### Через Neon Dashboard

1. Зайдите в Neon Dashboard
2. Проверьте:
   - ✅ База данных активна
   - ✅ Есть таблицы (через SQL Editor: `\dt`)

---

## 📊 Чек-лист работоспособности

### Backend (Railway)
- [ ] Healthcheck endpoint отвечает (`/health`)
- [ ] Сервер запускается без ошибок (проверьте логи)
- [ ] Переменные окружения установлены
- [ ] Подключение к базе данных работает
- [ ] API endpoints отвечают

### Frontend (Vercel)
- [ ] Страница загружается без ошибок
- [ ] Нет ошибок в консоли браузера
- [ ] API запросы идут на правильный URL
- [ ] Формы работают
- [ ] Можно зарегистрироваться/войти

### База данных (Neon)
- [ ] База данных доступна
- [ ] Таблицы созданы (миграции применены)
- [ ] Можно подключиться из Railway

---

## 🐛 Если что-то не работает

### Проблема: Healthcheck не отвечает

**Проверьте**:
1. Логи Railway - есть ли ошибки при запуске?
2. Переменные окружения - все установлены?
3. PORT - Railway устанавливает автоматически?

**Решение**: См. `TROUBLESHOOTING.md`

### Проблема: Frontend не подключается к API

**Проверьте**:
1. Vercel Dashboard → Settings → Environment Variables
2. `NEXT_PUBLIC_API_URL` установлен правильно?
3. URL БЕЗ `/api` в конце?

**Решение**: Обновите `NEXT_PUBLIC_API_URL` и передеплойте

### Проблема: "Cannot connect to database"

**Проверьте**:
1. `DATABASE_URL` в Railway Variables
2. Формат connection string правильный?
3. БД доступна извне (не localhost)?

**Решение**: Проверьте `DATABASE_URL` из Neon

---

## ✅ Все работает? Отлично!

Если все проверки пройдены:
1. ✅ Backend работает на Railway
2. ✅ Frontend работает на Vercel
3. ✅ База данных работает на Neon
4. ✅ Все подключено и работает

**Следующие шаги**:
- Обновите `YOUR_DEPLOYMENT.md` с URL вашего API
- Настройте домен (если нужно)
- Настройте мониторинг (опционально)

---

**Поздравляю с успешным деплоем! 🎉**

