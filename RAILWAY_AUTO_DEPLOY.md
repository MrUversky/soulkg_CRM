# 🚂 Railway - Автоматический деплой

## ✅ Как это работает

Railway автоматически редеплоится при каждом push в подключенный GitHub репозиторий, если:

1. ✅ Проект подключен через GitHub (не через Git URL)
2. ✅ В настройках проекта включен "Auto Deploy"
3. ✅ Push идет в ветку, за которой следит Railway (обычно `main`)

## 🔍 Проверка настроек

### 1. Проверьте подключение к GitHub

1. Зайдите в Railway Dashboard
2. Откройте ваш проект
3. Нажмите на вкладку **Settings**
4. Проверьте раздел **Source**
   - Должно быть: "Connected to GitHub"
   - Должен быть указан ваш репозиторий: `MrUversky/soulkg_CRM`

### 2. Проверьте Auto Deploy

В том же разделе Settings → Source:
- ✅ Должна быть включена опция **"Auto Deploy"**
- ✅ Должна быть выбрана ветка **"main"** (или та, в которую вы пушите)

### 3. Проверьте Webhook

Railway создает GitHub webhook автоматически. Чтобы проверить:

1. Зайдите в GitHub → ваш репозиторий
2. Settings → Webhooks
3. Должен быть webhook от Railway (railway.app)

## 🚀 Что происходит при push

Когда вы делаете `git push`:

1. **GitHub получает push**
2. **GitHub отправляет webhook в Railway**
3. **Railway запускает новый деплой**
4. **Railway собирает Docker образ** (используя `apps/api/Dockerfile`)
5. **Railway деплоит новый образ**

Вы увидите новый деплой в Railway Dashboard → Deployments.

## 📊 Мониторинг деплоя

### В Railway Dashboard:

1. Откройте ваш проект
2. Перейдите в **Deployments**
3. Вы увидите список всех деплоев:
   - ✅ Успешные (зеленые)
   - ⏳ В процессе (желтые)
   - ❌ Неудачные (красные)

### Просмотр логов:

1. Кликните на деплой
2. Перейдите в **Logs**
3. Вы увидите весь процесс сборки и деплоя

## 🔧 Если автоматический деплой не работает

### Проблема 1: Проект не подключен через GitHub

**Решение**:
1. Удалите текущий сервис (если создан через Git URL)
2. Создайте новый проект через "Deploy from GitHub repo"
3. Выберите ваш репозиторий

### Проблема 2: Auto Deploy выключен

**Решение**:
1. Settings → Source
2. Включите "Auto Deploy"
3. Выберите ветку `main`

### Проблема 3: Webhook не работает

**Решение**:
1. Settings → Source
2. Нажмите "Disconnect" и подключите заново
3. Или проверьте webhook в GitHub Settings → Webhooks

### Проблема 4: Push в другую ветку

**Решение**:
- Railway по умолчанию следит за `main`
- Либо пушите в `main`, либо измените настройки в Railway

## 🎯 Ручной редеплой

Если нужно задеплоить вручную:

1. Railway Dashboard → ваш проект
2. Перейдите в **Deployments**
3. Найдите последний деплой
4. Нажмите **"..."** → **"Redeploy"**

Или:

1. Settings → Source
2. Нажмите **"Redeploy"**

## 📝 Текущий статус

После вашего последнего push (`git push`):

1. ✅ Изменения отправлены в GitHub
2. ⏳ Railway должен автоматически начать деплой
3. 📊 Проверьте Railway Dashboard → Deployments

**Время деплоя**: обычно 3-5 минут (зависит от размера образа)

---

## 💡 Советы

1. **Всегда проверяйте логи** если деплой не удался
2. **Используйте feature branches** для тестирования перед merge в main
3. **Мониторьте Railway Dashboard** чтобы видеть статус деплоев
4. **Настройте уведомления** в Railway для получения email о статусе деплоев

---

**Проверьте сейчас**: Railway Dashboard → Deployments → должен быть новый деплой после вашего push!

