# 🔧 Настройка Vercel

## Проблема

Vercel не может найти Next.js, потому что он находится в `apps/web/`, а не в корне проекта.

## Решение

### Вариант 1: Настройка через Vercel Dashboard (Рекомендуется)

1. Зайдите в Vercel Dashboard → ваш проект
2. Settings → General
3. Найдите раздел **"Root Directory"**
4. Установите: `apps/web`
5. Сохраните изменения
6. Передеплойте проект

### Вариант 2: Использование vercel.json

Файл `vercel.json` уже настроен, но Vercel может его игнорировать если Root Directory не установлен.

**Важно**: Установите Root Directory в настройках проекта!

## Проверка

После настройки Root Directory:
1. Vercel должен автоматически определить Next.js
2. Build должен пройти успешно
3. Деплой должен завершиться без ошибок

## Переменные окружения

Не забудьте установить в Vercel Dashboard → Settings → Environment Variables:

```
NEXT_PUBLIC_API_URL=https://soul-kg-crm-api-production.up.railway.app
```

**БЕЗ `/api` в конце!**

---

**После настройки Root Directory передеплойте проект.**

